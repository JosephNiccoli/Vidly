@*specify directive  ex @model Vidly.Models.Movie*@
@model Vidly.Models.Movie
	@{
		ViewBag.Title = "Random";
		Layout = "~/Views/Shared/_Layout.cshtml";
	}

	<h2>@Model.Name</h2>  @*every view has the model which is passed to the controller*@

